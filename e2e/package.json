{
  "name": "beacon2026-e2e",
  "private": true,
  "type": "module",
  "description": "End-to-end user tests for beacon2026 — run against a staging environment after each deployment.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "install-browsers": "playwright install chromium"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "dotenv": "^16.4.5"
  }
}
