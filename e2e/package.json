{
  "name": "beacon2-e2e",
  "private": true,
  "type": "module",
  "description": "End-to-end user tests for Beacon2 — run against a staging environment after each deployment.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "install-browsers": "playwright install chromium"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "dotenv": "^16.4.5"
  }
}
